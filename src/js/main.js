// const hover = (event) => {
//   const action = event.type;
//   const element = Array.from(event.target.classList.children);
//   if (action === 'mouseenter') {
//     element.forEach(item
//   }
//   if (action === 'mouseleave') {

//   }  
//   return action === 'mouseenter' ? element.add('hover') : element.remove('hover');
// };

// const projects = Array.from(document.getElementsByClassName('project'));
// projects.forEach((project) => {
//   project.addEventListener('mouseenter', hover);
//   project.addEventListener('mouseleave', hover);
// });

